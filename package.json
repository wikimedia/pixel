{
  "private": true,
  "name": "pixel",
  "scripts": {
    "start": "docker-compose up -d",
    "bash": "docker-compose exec mediawiki bash",
    "reference": "docker-compose exec mediawiki bash ./update-mediawiki.sh && docker-compose run pixel reference --config backstop.config.js",
    "test": "docker-compose run pixel test --config backstop.config.js && app/backstop_data/html_report/index.html; open app/backstop_data/html_report/index.html",
    "stop": "docker-compose down",
    "clean": "docker-compose down --rmi all -v",
    "db:save": "docker run --rm --volumes-from pixel-database-1 -v $(pwd)/backups:/backups ubuntu tar cvfz /backups/database_$(date '+%Y-%m-%d_%H-%M-%S%z(%Z)').tar.gz var/lib/mysql"
  }
}
